# 题库架构改造 实施计划

> **给执行者：** 用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务逐个执行。步骤用 `- [ ]` 复选框跟踪。

**目标：** 把 56.6 万字符的单文件 HTML 拆成「SQLite 数据源 + 15 个内容页 + 首页壳」，并让题型从全单选扩到单选/判断/多选。

**架构：** `bank.db`（SQLite）是唯一真相源；`server.py`（纯 Python 标准库）读库并提供 JSON API；`web/` 下每个页面只装自己那部分内容。状态（作答、错题本、模考）全部搬到服务端，不再依赖 localStorage 的跨页共享。

**技术栈：** Python 3 标准库（`sqlite3` / `http.server` / `json` / `unittest`）、原生 JS。**零第三方依赖，不装任何 pip 包。**

**设计文档：** [docs/题库架构改造-design.md](题库架构改造-design.md) —— 本计划实现它，两者一起读。

---

## 全局约束

- **零第三方依赖。** 测试用 `unittest`，不用 pytest；服务用 `http.server`，不用 Flask。任何一步引入 pip 包都是错的。
- **现有 99 条冒烟断言是硬验收。** 第 2 步拆页前后必须全绿，一条都不许少、不许改弱。
- **判分函数只有一份实现**，在服务端 `grade()`。客户端不做任何判分。
- **答案和解析随题目一起下发。** 这是自学工具，不设防作弊。
- **每条新断言都要做突变验证**：注入一个突变证明它真的会红，然后还原。**测不出红的断言不算数，不算完成。**
- **不删 `考点体系.html`。** 它是回归对照，一直留到第 3 步结束。
- **中文注释。** 与现有代码风格一致。
- **提交粒度**：每个任务末尾一次提交。
- **不要在 `web/` 里重复写导航 HTML**，导航由 `nav.js` 注入。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `schema.sql` | 建表 DDL。唯一的结构定义 |
| `import_bank.py` | `题库.json` → `bank.db`。含全部校验，**不通过就整体拒绝** |
| `export_bank.py` | `bank.db` → `题库.json`。用于备份和一致性比对 |
| `server.py` | HTTP 服务：JSON API + 静态文件 |
| `banklib.py` | 判分、取题、统计等纯函数。**不碰 HTTP**，方便单测 |
| `tests/test_schema.py` | 表约束、级联删除 |
| `tests/test_grade.py` | 三题型判分 |
| `tests/test_import.py` | 8 条导入校验，每条构造坏数据证明被拒 |
| `tests/test_roundtrip.py` | DB → JSON 与现有 `题库.json` 逐字段比对 |
| `tests/test_server.py` | API 端点（起真实服务，打真实 HTTP） |
| `tests/smoke.js` | 从 `tools/smoke.js` 迁来，改成针对拆页后的引擎 |
| `web/_template.html` | 页面模板，含 `<!--NAV-->` `<!--BODY-->` 占位 |
| `web/assets/app.css` | 现有样式，三态主题原样搬 |
| `web/assets/nav.js` | 导航注入 + 主题切换 + 当前页高亮 |
| `web/assets/bank.js` | fetch 封装 |
| `web/assets/quiz.js` `mock.js` `wrong.js` | 三个引擎 |
| `tools/split_pages.py` | 一次性：从 `考点体系.html` 抽出 15 个内容页 |

### 模块 → 页面（section）映射

导入时按这张表给每题填 `section`。**这张表是唯一来源，改这里就够。**

| module | section |
|---|---|
| 教育学 / 教育心理学 / 教育法律法规 / 教师职业理念与职业道德 / 职业教育与高等教育 | `edu` |
| 公共基础 · 政治与时政 / 法律 / 公文写作 / 经济、管理与常识 / 人文历史与科技常识 | `pub` |
| 高校辅导员 | `counselor` |
| 唐山工业职业技术大学校情 | `school` |
| 唐山本地政策与时政 | `local` |

---

## 关于 `bank.db` 是否入库

**提交进 git。** 它是真相源，不提交就没意义。

代价：SQLite 是二进制，git 无法 diff，每次改题都是一个新 blob。这个项目个人用、改动量不大，可以接受。

配套：`题库.json` 仍然提交，由 `export_bank.py` 生成。它是可读、可 diff 的镜像，`tests/test_roundtrip.py` 保证两者一致——**不一致就是有 bug**。

---

# 第 1 步：数据层

页面完全不动，你照常学习。这一步只把题库搬进数据库。

---

### 任务 1：建表与约束

**文件：**
- 创建：`schema.sql`
- 创建：`tests/test_schema.py`

**接口：**
- 产出：`schema.sql` 里的 5 张表：`questions` / `attempts` / `wrong` / `mock_runs` / `sprint`。后续所有任务都依赖这些表名和列名。

- [ ] **步骤 1：先写失败的测试**

创建 `tests/test_schema.py`：

```python
# -*- coding: utf-8 -*-
"""表结构约束：类型白名单、唯一性、级联删除。"""
import os
import sqlite3
import tempfile
import unittest

SCHEMA = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), 'schema.sql')


def fresh_db():
    """建一个空库，返回连接。"""
    fd, path = tempfile.mkstemp(suffix='.db')
    os.close(fd)
    conn = sqlite3.connect(path)
    conn.executescript(open(SCHEMA, encoding='utf-8').read())
    return conn


def add_q(conn, qid='e101', n=1, qtype='single', answer='B'):
    conn.execute(
        "INSERT INTO questions (id,n,section,module,type,stem,options,answer,explanation) "
        "VALUES (?,?,?,?,?,?,?,?,?)",
        (qid, n, 'edu', '教育学', qtype, '题干', '["甲","乙","丙","丁"]', answer, '解析'))


class TestSchema(unittest.TestCase):
    def test_五张表都在(self):
        conn = fresh_db()
        names = {r[0] for r in conn.execute(
            "SELECT name FROM sqlite_master WHERE type='table'")}
        for t in ('questions', 'attempts', 'wrong', 'mock_runs', 'sprint'):
            self.assertIn(t, names)

    def test_type_只认三种(self):
        conn = fresh_db()
        with self.assertRaises(sqlite3.IntegrityError):
            add_q(conn, qtype='essay')

    def test_id_唯一(self):
        conn = fresh_db()
        add_q(conn)
        with self.assertRaises(sqlite3.IntegrityError):
            add_q(conn, n=2)

    def test_n_唯一(self):
        conn = fresh_db()
        add_q(conn, qid='e101', n=1)
        with self.assertRaises(sqlite3.IntegrityError):
            add_q(conn, qid='e102', n=1)

    def test_删题会连带删掉作答和错题(self):
        conn = fresh_db()
        conn.execute('PRAGMA foreign_keys = ON')
        add_q(conn)
        conn.execute("INSERT INTO attempts VALUES ('e101','C',0,'2026-09-12')")
        conn.execute("INSERT INTO wrong VALUES ('e101','C',1,'2026-09-12','2026-09-12',0)")
        conn.execute("DELETE FROM questions WHERE id='e101'")
        self.assertEqual(conn.execute("SELECT COUNT(*) FROM attempts").fetchone()[0], 0)
        self.assertEqual(conn.execute("SELECT COUNT(*) FROM wrong").fetchone()[0], 0)


if __name__ == '__main__':
    unittest.main()
```

- [ ] **步骤 2：跑测试，确认它失败**

```bash
python3 -m unittest tests.test_schema -v
```

预期：`FileNotFoundError`，因为 `schema.sql` 还不存在。

- [ ] **步骤 3：写 `schema.sql`**

```sql
-- 题库与学习数据。bank.db 的唯一结构定义。
PRAGMA foreign_keys = ON;

CREATE TABLE questions (
  id          TEXT PRIMARY KEY,
  n           INTEGER NOT NULL UNIQUE,      -- 稳定序号，仅用于显示题号
  section     TEXT NOT NULL,                -- 归哪个知识页：edu/pub/local/school/counselor
  module      TEXT NOT NULL,                -- 配比表与成绩单按它分组
  type        TEXT NOT NULL CHECK (type IN ('single','judge','multi')),
  stem        TEXT NOT NULL,
  options     TEXT NOT NULL,                -- JSON 数组。判断题固定 ["正确","错误"]
  answer      TEXT NOT NULL,                -- 'B' / 'A' / 'ABD'（升序无分隔）
  explanation TEXT NOT NULL,
  source      TEXT,                         -- 本地题/时政题必填
  tags        TEXT,
  difficulty  INTEGER CHECK (difficulty BETWEEN 1 AND 3),
  batch       INTEGER NOT NULL DEFAULT 1
);
CREATE INDEX idx_q_section ON questions(section);
CREATE INDEX idx_q_module  ON questions(module);
CREATE INDEX idx_q_type    ON questions(type);

CREATE TABLE attempts (
  qid     TEXT PRIMARY KEY REFERENCES questions(id) ON DELETE CASCADE,
  chosen  TEXT NOT NULL,
  correct INTEGER NOT NULL,                 -- 判分时算好存下，统计查询快
  at      TEXT NOT NULL
);

CREATE TABLE wrong (
  qid         TEXT PRIMARY KEY REFERENCES questions(id) ON DELETE CASCADE,
  chosen      TEXT,
  wrong_count INTEGER NOT NULL DEFAULT 1,
  first_at    TEXT NOT NULL,
  last_at     TEXT NOT NULL,
  resolved    INTEGER NOT NULL DEFAULT 0
);

CREATE TABLE mock_runs (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  started_at   TEXT NOT NULL,
  ends_at      TEXT NOT NULL,
  submitted_at TEXT,
  state        TEXT NOT NULL                -- JSON：题号序列+答案+分部分结构+是否含多选判断
);

CREATE TABLE sprint (
  key   TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  at    TEXT NOT NULL
);
```

> `difficulty` 的 CHECK 不用写 `IS NULL`：SQLite 的 CHECK 只在表达式为**假**时失败，NULL 不算假，所以 NULL 天然通过。

- [ ] **步骤 4：跑测试，确认通过**

```bash
python3 -m unittest tests.test_schema -v
```

预期：5 个测试全 PASS。

- [ ] **步骤 5：突变验证**

把 `schema.sql` 里 `type` 的 `CHECK` 约束删掉，重跑：

```bash
python3 -m unittest tests.test_schema -v
```

预期：`test_type_只认三种` **变红**。变红后把约束加回去，确认重新全绿。

- [ ] **步骤 6：提交**

```bash
git add schema.sql tests/test_schema.py
git commit -m "数据层：建表 DDL 与约束测试"
```

---

### 任务 2：导入校验

**文件：**
- 创建：`import_bank.py`
- 创建：`tests/test_import.py`

**接口：**
- 消费：`schema.sql` 的 5 张表
- 产出：
  - `SECTION_OF: dict[str, str]` —— 模块 → section 映射
  - `validate(questions: list[dict]) -> list[str]` —— 返回错误信息列表，空列表表示通过
  - `import_bank(json_path, db_path, batch=1) -> int` —— 返回入库题数

- [ ] **步骤 1：先写失败的测试**

创建 `tests/test_import.py`。每条校验都要有一个**构造坏数据证明它被拒**的测试：

```python
# -*- coding: utf-8 -*-
"""导入校验：8 条规则，每条都要有一个「坏数据被拒」的测试。"""
import copy
import unittest

from import_bank import validate, SECTION_OF

GOOD = {
    'id': 'e101', 'n': 1, 'module': '教育学', 'stem': '题干',
    'options': [{'key': 'A', 'text': '甲'}, {'key': 'B', 'text': '乙'},
                {'key': 'C', 'text': '丙'}, {'key': 'D', 'text': '丁'}],
    'answer': 'B', 'explanation': '解析',
}


def bad(**kw):
    q = copy.deepcopy(GOOD)
    for k, v in kw.items():
        if v is None:
            q.pop(k, None)
        else:
            q[k] = v
    return [q]


class TestValidate(unittest.TestCase):
    def test_好数据通过(self):
        self.assertEqual(validate([copy.deepcopy(GOOD)]), [])

    def test_拒绝重复id(self):
        errs = validate([copy.deepcopy(GOOD), copy.deepcopy(GOOD)])
        # 第二条 n 也得改，否则先撞 n 唯一——这里显式只关心 id
        self.assertTrue(any('重复' in e and 'id' in e for e in errs), errs)

    def test_拒绝未知模块(self):
        self.assertTrue(validate(bad(module='不存在的模块')))

    def test_拒绝答案字母不在选项里(self):
        self.assertTrue(validate(bad(answer='E')))

    def test_拒绝多选答案未升序(self):
        self.assertTrue(validate(bad(answer='BA')))

    def test_拒绝多选答案有重复字母(self):
        self.assertTrue(validate(bad(answer='AA')))

    def test_拒绝判断题选项数不是二(self):
        self.assertTrue(validate(bad(answer='A')))

    def test_拒绝空解析(self):
        self.assertTrue(validate(bad(explanation='   ')))

    def test_拒绝本地题没有来源(self):
        q = copy.deepcopy(GOOD)
        q['module'] = '唐山本地政策与时政'
        self.assertTrue(any('来源' in e for e in validate([q])))

    def test_拒绝解析与答案不自洽(self):
        # 解析写「故选 C」，答案却是 B
        self.assertTrue(validate(bad(explanation='故选 C。')))


class TestSectionMap(unittest.TestCase):
    def test_映射覆盖全部13个模块(self):
        mods = ['教育学', '教育心理学', '教育法律法规', '教师职业理念与职业道德',
                '公共基础 · 政治与时政', '公共基础 · 法律', '公共基础 · 公文写作',
                '公共基础 · 经济、管理与常识', '高校辅导员', '职业教育与高等教育',
                '人文历史与科技常识', '唐山工业职业技术大学校情', '唐山本地政策与时政']
        for m in mods:
            self.assertIn(m, SECTION_OF, f'模块「{m}」没有 section 映射')


if __name__ == '__main__':
    unittest.main()
```

- [ ] **步骤 2：跑测试，确认它失败**

```bash
python3 -m unittest tests.test_import -v
```

预期：`ModuleNotFoundError: No module named 'import_bank'`。

- [ ] **步骤 3：写 `import_bank.py`**

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""把题库 JSON 导入 bank.db。全部校验通过才写，不部分写入。

用法：
    python3 import_bank.py                     # 题库.json → bank.db，整批替换
    python3 import_bank.py --batch 2           # 标成第 2 批
"""
import argparse
import json
import os
import re
import sqlite3
import sys

BASE = os.path.dirname(os.path.abspath(__file__))
DB = os.path.join(BASE, 'bank.db')
SCHEMA = os.path.join(BASE, 'schema.sql')

# 模块 → 知识页。加模块时改这里，别改别处。
SECTION_OF = {
    '教育学': 'edu',
    '教育心理学': 'edu',
    '教育法律法规': 'edu',
    '教师职业理念与职业道德': 'edu',
    '职业教育与高等教育': 'edu',
    '公共基础 · 政治与时政': 'pub',
    '公共基础 · 法律': 'pub',
    '公共基础 · 公文写作': 'pub',
    '公共基础 · 经济、管理与常识': 'pub',
    '人文历史与科技常识': 'pub',
    '高校辅导员': 'counselor',
    '唐山工业职业技术大学校情': 'school',
    '唐山本地政策与时政': 'local',
}

# 必须附来源的模块：内容是联网核实来的
NEEDS_SOURCE = {'唐山工业职业技术大学校情', '唐山本地政策与时政'}

LETTERS = 'ABCDE'


def infer_type(q):
    """老题库没有 type 字段。选项数 2 就是判断题，其余一律单选。"""
    return 'judge' if len(q.get('options') or []) == 2 else 'single'


def validate(questions):
    """返回错误信息列表。空列表 = 全部通过。"""
    errs = []
    seen_id, seen_n = set(), set()
    for q in questions:
        qid = q.get('id', '(缺 id)')
        where = f'[{qid}]'

        if qid in seen_id:
            errs.append(f'{where} id 重复')
        seen_id.add(qid)

        n = q.get('n')
        if n in seen_n:
            errs.append(f'{where} n={n} 重复')
        seen_n.add(n)

        mod = q.get('module')
        if mod not in SECTION_OF:
            errs.append(f'{where} 未知模块「{mod}」，需先在 SECTION_OF 里登记')

        opts = q.get('options') or []
        keys = [o.get('key') for o in opts]
        qtype = q.get('type') or infer_type(q)

        if qtype == 'judge':
            if len(opts) != 2:
                errs.append(f'{where} 判断题必须恰好 2 个选项，实际 {len(opts)}')
        elif len(opts) < 2:
            errs.append(f'{where} 选项少于 2 个')

        ans = (q.get('answer') or '').strip()
        if not ans:
            errs.append(f'{where} 缺 answer')
        else:
            if qtype == 'multi':
                if len(ans) < 2:
                    errs.append(f'{where} 多选题答案「{ans}」至少要有 2 个字母')
                if len(set(ans)) != len(ans):
                    errs.append(f'{where} 多选题答案「{ans}」有重复字母')
                if list(ans) != sorted(ans):
                    errs.append(f'{where} 多选题答案「{ans}」没有按字母升序')
            else:
                if len(ans) != 1:
                    errs.append(f'{where} {qtype} 答案「{ans}」应只有 1 个字母')
            for ch in ans:
                if ch not in keys:
                    errs.append(f'{where} 答案字母「{ch}」不在选项 {keys} 里')

        exp = (q.get('explanation') or '').strip()
        if not exp:
            errs.append(f'{where} 解析为空')
        else:
            # 解析里「故选 X」的 X 必须就是答案——多字母也认
            for m in re.finditer(r'故[选是]?\s*([A-E]{1,5})(?![A-Za-z])', exp):
                if m.group(1) != ans:
                    errs.append(
                        f'{where} 解析写「{m.group(0).strip()}」但答案是 {ans}')

        if mod in NEEDS_SOURCE and not (q.get('source') or '').strip():
            errs.append(f'{where} 「{mod}」的题必须附来源 source')

    return errs


def import_bank(json_path, db_path=DB, batch=1, replace=True):
    data = json.load(open(json_path, encoding='utf-8'))
    questions = data['questions'] if isinstance(data, dict) else data

    for q in questions:
        q.setdefault('type', infer_type(q))

    errs = validate(questions)
    if errs:
        print(f'校验未通过，共 {len(errs)} 条，未写入任何数据：', file=sys.stderr)
        for e in errs[:30]:
            print('  ' + e, file=sys.stderr)
        if len(errs) > 30:
            print(f'  …还有 {len(errs) - 30} 条', file=sys.stderr)
        return 0

    conn = sqlite3.connect(db_path)
    conn.executescript(open(SCHEMA, encoding='utf-8').read())
    if replace:
        conn.execute('DELETE FROM questions')
    for q in questions:
        conn.execute(
            "INSERT INTO questions "
            "(id,n,section,module,type,stem,options,answer,explanation,"
            " source,tags,difficulty,batch) "
            "VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)",
            (q['id'], q['n'], SECTION_OF[q['module']], q['module'], q['type'],
             q['stem'], json.dumps(q['options'], ensure_ascii=False), q['answer'],
             q['explanation'], q.get('source'), q.get('tags'),
             q.get('difficulty'), batch))
    conn.commit()
    conn.close()
    return len(questions)


if __name__ == '__main__':
    ap = argparse.ArgumentParser()
    ap.add_argument('json_path', nargs='?', default=os.path.join(BASE, '题库.json'))
    ap.add_argument('--db', default=DB)
    ap.add_argument('--batch', type=int, default=1)
    a = ap.parse_args()
    n = import_bank(a.json_path, a.db, a.batch)
    if n:
        print(f'✅ 入库 {n} 题 → {a.db}')
    else:
        sys.exit(1)
```

- [ ] **步骤 4：跑测试，确认通过**

```bash
python3 -m unittest tests.test_import -v
```

预期：11 个测试全 PASS。

- [ ] **步骤 5：突变验证**

把 `validate()` 里这段注释掉：

```python
                if list(ans) != sorted(ans):
                    errs.append(f'{where} 多选题答案「{ans}」没有按字母升序')
```

重跑，预期 `test_拒绝多选答案未升序` **变红**。还原后重新全绿。

对 `NEEDS_SOURCE` 那段做同样的操作，预期 `test_拒绝本地题没有来源` 变红。

- [ ] **步骤 6：提交**

```bash
git add import_bank.py tests/test_import.py
git commit -m "数据层：题库导入与 8 条校验规则"
```

---

### 任务 3：现有 1000 题入库

**文件：**
- 修改：`import_bank.py`（若上一步的校验在真实数据上报错，修的是校验或数据，不是放宽规则）
- 创建：`bank.db`

**接口：**
- 消费：`import_bank.import_bank()`
- 产出：`bank.db`，含 1000 行 `questions`

- [ ] **步骤 1：先跑一次，看真实数据是不是过得了校验**

```bash
python3 import_bank.py
```

预期：**大概率会有报错。** 这是好事——校验在真实数据上发现了问题。逐条看：

- 「解析写「故选 X」但答案是 Y」→ 核对这道题，**改数据或改解析**，别放宽正则。
- 「未知模块」→ 说明 `SECTION_OF` 漏了模块，补上。
- 其他 → 逐条判断是数据错还是校验太严。

- [ ] **步骤 2：确认入库结果**

```bash
python3 - <<'PY'
import sqlite3
c = sqlite3.connect('bank.db')
print('题数:', c.execute('SELECT COUNT(*) FROM questions').fetchone()[0])
print('题型:', c.execute('SELECT type, COUNT(*) FROM questions GROUP BY type').fetchall())
print('section 分布:', c.execute('SELECT section, COUNT(*) FROM questions GROUP BY section').fetchall())
print('无 section 的:', c.execute('SELECT COUNT(*) FROM questions WHERE section IS NULL OR section=""').fetchone()[0])
PY
```

预期：题数 1000；题型全部 `single`；`section` 分布符合映射表；无 section 的为 0。

- [ ] **步骤 3：提交**

```bash
git add bank.db import_bank.py
git commit -m "数据层：现有 1000 题入库"
```

---

### 任务 4：导出与一致性比对

**文件：**
- 创建：`export_bank.py`
- 创建：`tests/test_roundtrip.py`

**接口：**
- 消费：`bank.db` 的 `questions` 表
- 产出：`export_bank.export(db_path) -> dict`，结构为 `{'version':..., 'total':..., 'questions':[...]}`，字段与现有 `题库.json` 一致

- [ ] **步骤 1：先写失败的测试**

创建 `tests/test_roundtrip.py`：

```python
# -*- coding: utf-8 -*-
"""DB 导出的 JSON 必须与现有 题库.json 逐字段一致——不一致就是有 bug。"""
import json
import os
import unittest

from export_bank import export

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DB = os.path.join(BASE, 'bank.db')
ORIG = os.path.join(BASE, '题库.json')

# 刻意不含 type / section：这两个是这次新加的，老 题库.json 里没有，
# 加进来必然对不上。要比的是「原有字段在搬家过程中有没有被改坏」。
FIELDS = ['id', 'n', 'module', 'stem', 'options', 'answer', 'explanation']


class TestRoundtrip(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        cls.got = export(DB)
        cls.orig = json.load(open(ORIG, encoding='utf-8'))

    def test_题数一致(self):
        self.assertEqual(self.got['total'], self.orig['total'])
        self.assertEqual(len(self.got['questions']), len(self.orig['questions']))

    def test_逐字段一致(self):
        a = {q['id']: q for q in self.got['questions']}
        b = {q['id']: q for q in self.orig['questions']}
        self.assertEqual(set(a), set(b), 'id 集合不同')
        bad = []
        for qid in b:
            for f in FIELDS:
                if a[qid].get(f) != b[qid].get(f):
                    bad.append(f'{qid}.{f}: DB={a[qid].get(f)!r} JSON={b[qid].get(f)!r}')
        self.assertEqual(bad, [], f'{len(bad)} 处不一致，前 5 条：\n' + '\n'.join(bad[:5]))

    def test_顺序按_n_排(self):
        ns = [q['n'] for q in self.got['questions']]
        self.assertEqual(ns, sorted(ns))


if __name__ == '__main__':
    unittest.main()
```

- [ ] **步骤 2：跑测试，确认它失败**

```bash
python3 -m unittest tests.test_roundtrip -v
```

预期：`ModuleNotFoundError: No module named 'export_bank'`。

- [ ] **步骤 3：写 `export_bank.py`**

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""bank.db → 题库.json。

题库.json 是可读、可 diff 的镜像，真相源仍是 bank.db。
tests/test_roundtrip.py 保证两者一致。
"""
import argparse
import json
import os
import sqlite3
import sys

BASE = os.path.dirname(os.path.abspath(__file__))
DB = os.path.join(BASE, 'bank.db')


def export(db_path=DB):
    conn = sqlite3.connect(db_path)
    conn.row_factory = sqlite3.Row
    rows = conn.execute(
        "SELECT id,n,module,type,stem,options,answer,explanation,source,tags,difficulty "
        "FROM questions ORDER BY n").fetchall()
    conn.close()

    questions = []
    for r in rows:
        q = {
            'id': r['id'], 'n': r['n'], 'module': r['module'], 'type': r['type'],
            'stem': r['stem'], 'options': json.loads(r['options']),
            'answer': r['answer'], 'explanation': r['explanation'],
        }
        # 可选字段：有值才写，保持和现有 JSON 一样的形状
        for k in ('source', 'tags', 'difficulty'):
            if r[k] is not None:
                q[k] = r[k]
        questions.append(q)

    return {
        '_note': '唐山工业职业技术大学 2026 选聘 · 教育类岗位 笔试题库。'
                 '本文件由 export_bank.py 从 bank.db 生成，不要手改——改 bank.db。',
        'version': '2026-09-12',
        'total': len(questions),
        'questions': questions,
    }


if __name__ == '__main__':
    ap = argparse.ArgumentParser()
    ap.add_argument('--db', default=DB)
    ap.add_argument('-o', '--out', default=os.path.join(BASE, '题库.json'))
    a = ap.parse_args()
    data = export(a.db)
    with open(a.out, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=1)
        f.write('\n')
    print(f'✅ 导出 {data["total"]} 题 → {a.out}')
```

- [ ] **步骤 4：跑测试，确认通过**

```bash
python3 -m unittest tests.test_roundtrip -v
```

预期：3 个测试全 PASS。**若 `test_逐字段一致` 失败**，说明导入或导出丢了字段——修代码，不要去改测试的 `FIELDS` 列表。

- [ ] **步骤 5：突变验证**

把 `export()` 里 `'answer': r['answer']` 改成 `'answer': r['answer'].upper() + 'X'`，重跑，预期 `test_逐字段一致` 变红。还原。

- [ ] **步骤 6：提交**

```bash
git add export_bank.py tests/test_roundtrip.py 题库.json
git commit -m "数据层：DB 导出与逐字段一致性测试"
```

---

# 第 2 步：拆页与服务

**这一步的硬验收：99 条冒烟断言全绿，一条不少。** 行为必须与现在完全等价。

---

### 任务 5：判分与取数纯函数

先做 `banklib.py`——不碰 HTTP，纯函数，好测。它是后面所有东西的基础。

**文件：**
- 创建：`banklib.py`
- 创建：`tests/test_grade.py`

**接口：**
- 产出：
  - `grade(qtype: str, answer: str, chosen: str) -> bool`
  - `record_attempt(conn, qid, chosen) -> dict` —— 返回 `{correct, answer, explanation, wrong_count}`
  - `list_questions(conn, section=None, module=None, qtype=None, limit=40, offset=0) -> dict`

- [ ] **步骤 1：先写失败的测试**

创建 `tests/test_grade.py`：

```python
# -*- coding: utf-8 -*-
"""三题型判分。多选题全对才算对，不设部分分。"""
import unittest

from banklib import grade


class TestGrade(unittest.TestCase):
    def test_单选答对(self):
        self.assertTrue(grade('single', 'B', 'B'))

    def test_单选答错(self):
        self.assertFalse(grade('single', 'B', 'C'))

    def test_判断题走同一套(self):
        self.assertTrue(grade('judge', 'A', 'A'))
        self.assertFalse(grade('judge', 'A', 'B'))

    def test_多选全对才算对(self):
        self.assertTrue(grade('multi', 'ABD', 'ABD'))

    def test_多选少选算错(self):
        self.assertFalse(grade('multi', 'ABD', 'AB'))

    def test_多选多选算错(self):
        self.assertFalse(grade('multi', 'ABD', 'ABCD'))

    def test_多选错选算错(self):
        self.assertFalse(grade('multi', 'ABD', 'ABC'))

    def test_多选顺序不影响判分(self):
        self.assertTrue(grade('multi', 'ABD', 'DBA'))

    def test_没作答算错(self):
        for t in ('single', 'judge', 'multi'):
            self.assertFalse(grade(t, 'B', ''))


if __name__ == '__main__':
    unittest.main()
```

- [ ] **步骤 2：跑测试，确认它失败**

```bash
python3 -m unittest tests.test_grade -v
```

预期：`ModuleNotFoundError: No module named 'banklib'`。

- [ ] **步骤 3：写 `banklib.py` 的 `grade()`**

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""取题、判分、统计。纯函数，不碰 HTTP——方便单测，也保证判分只有一份实现。"""
import json


def _norm(s):
    """把作答/答案规范化成可比较的形式：去空白、转大写、按字母排序。
    多选题的顺序不该影响判分。"""
    return ''.join(sorted(set((s or '').strip().upper())))


def grade(qtype, answer, chosen):
    """三题型共用。多选全对才算对——少选、多选、错选一律算错。

    判断题其实和单选同构（答案就是单个字母 A/B），走同一条路。
    """
    if not chosen:
        return False
    return _norm(answer) == _norm(chosen)
```

- [ ] **步骤 4：跑测试，确认通过**

```bash
python3 -m unittest tests.test_grade -v
```

预期：9 个测试全 PASS。

- [ ] **步骤 5：突变验证**

把 `_norm` 改成 `return (s or '').strip().upper()`（去掉排序和去重），重跑：

预期：`test_多选顺序不影响判分` 变红。还原。

再把 `grade` 改成 `return _norm(chosen).startswith(_norm(answer))`（模拟"少选也算对"），预期 `test_多选少选算错` 变红。还原。

- [ ] **步骤 6：补 `record_attempt` 与 `list_questions`**

在 `banklib.py` 追加：

```python
def record_attempt(conn, qid, chosen):
    """写作答记录并维护错题本。返回本次判分结果。

    重做同一题会覆盖 attempts 那一行：
      做对了 → wrong.resolved = 1（保留历史，但不再算「还没掌握」）
      又错了 → wrong_count += 1
    """
    row = conn.execute(
        "SELECT type, answer, explanation FROM questions WHERE id=?", (qid,)).fetchone()
    if row is None:
        raise KeyError(f'题库里没有这道题：{qid}')
    qtype, answer, explanation = row

    ok = grade(qtype, answer, chosen)
    now = _now()
    conn.execute(
        "INSERT INTO attempts (qid,chosen,correct,at) VALUES (?,?,?,?) "
        "ON CONFLICT(qid) DO UPDATE SET chosen=excluded.chosen, "
        "correct=excluded.correct, at=excluded.at",
        (qid, chosen, 1 if ok else 0, now))

    if ok:
        conn.execute("UPDATE wrong SET resolved=1, last_at=? WHERE qid=?", (now, qid))
    else:
        conn.execute(
            "INSERT INTO wrong (qid,chosen,wrong_count,first_at,last_at,resolved) "
            "VALUES (?,?,1,?,?,0) "
            "ON CONFLICT(qid) DO UPDATE SET chosen=excluded.chosen, "
            "wrong_count=wrong.wrong_count+1, last_at=excluded.last_at, resolved=0",
            (qid, chosen, now, now))
    conn.commit()

    wc = conn.execute("SELECT wrong_count FROM wrong WHERE qid=?", (qid,)).fetchone()
    return {'correct': ok, 'answer': answer, 'explanation': explanation,
            'wrong_count': wc[0] if wc else 0}


def _now():
    import datetime
    return datetime.datetime.now().astimezone().isoformat(timespec='seconds')


def list_questions(conn, section=None, module=None, qtype=None,
                   limit=40, offset=0, include_answer=True):
    """取题。刷题页一次只取一页——1000 题全量下发会卡住手机。"""
    where, args = [], []
    for col, val in (('section', section), ('module', module), ('type', qtype)):
        if val:
            where.append(f'{col}=?')
            args.append(val)
    clause = ('WHERE ' + ' AND '.join(where)) if where else ''

    total = conn.execute(f'SELECT COUNT(*) FROM questions {clause}', args).fetchone()[0]
    cols = ('id,n,section,module,type,stem,options,answer,explanation,source,tags,difficulty'
            if include_answer else
            'id,n,section,module,type,stem,options,source,tags,difficulty')
    rows = conn.execute(
        f'SELECT {cols} FROM questions {clause} ORDER BY n LIMIT ? OFFSET ?',
        args + [limit, offset]).fetchall()

    names = cols.split(',')
    out = []
    for r in rows:
        q = dict(zip(names, r))
        q['options'] = json.loads(q['options'])
        out.append(q)
    return {'total': total, 'questions': out}
```

- [ ] **步骤 7：给新函数补测试**

在 `tests/test_grade.py` 追加：

```python
class TestRecordAttempt(unittest.TestCase):
    def setUp(self):
        import os, tempfile, sqlite3
        fd, self.path = tempfile.mkstemp(suffix='.db'); os.close(fd)
        self.conn = sqlite3.connect(self.path)
        base = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
        self.conn.executescript(open(os.path.join(base, 'schema.sql'), encoding='utf-8').read())
        self.conn.execute(
            "INSERT INTO questions (id,n,section,module,type,stem,options,answer,explanation) "
            "VALUES ('e101',1,'edu','教育学','single','题干','[\"甲\",\"乙\"]','B','解析')")
        self.conn.commit()

    def test_答错进错题本(self):
        from banklib import record_attempt
        r = record_attempt(self.conn, 'e101', 'A')
        self.assertFalse(r['correct'])
        self.assertEqual(r['wrong_count'], 1)

    def test_再错一次计数累加(self):
        from banklib import record_attempt
        record_attempt(self.conn, 'e101', 'A')
        r = record_attempt(self.conn, 'e101', 'A')
        self.assertEqual(r['wrong_count'], 2)

    def test_做对了标记已订正(self):
        from banklib import record_attempt
        record_attempt(self.conn, 'e101', 'A')
        record_attempt(self.conn, 'e101', 'B')
        resolved = self.conn.execute(
            "SELECT resolved FROM wrong WHERE qid='e101'").fetchone()[0]
        self.assertEqual(resolved, 1)

    def test_attempts_每题一行(self):
        from banklib import record_attempt
        record_attempt(self.conn, 'e101', 'A')
        record_attempt(self.conn, 'e101', 'B')
        n = self.conn.execute("SELECT COUNT(*) FROM attempts").fetchone()[0]
        self.assertEqual(n, 1)
```

- [ ] **步骤 8：跑测试，确认通过**

```bash
python3 -m unittest tests.test_grade -v
```

预期：13 个测试全 PASS。

- [ ] **步骤 9：提交**

```bash
git add banklib.py tests/test_grade.py
git commit -m "服务层：判分、作答记录、取数纯函数"
```

---

### 任务 6：HTTP 服务与 API

**文件：**
- 创建：`server.py`
- 创建：`tests/test_server.py`

**接口：**
- 消费：`banklib.grade` / `record_attempt` / `list_questions`
- 产出：可运行的 `python3 server.py`，监听 `0.0.0.0:8000`

- [ ] **步骤 1：先写失败的测试**

创建 `tests/test_server.py`。**起真实服务、打真实 HTTP**，不用 mock：

```python
# -*- coding: utf-8 -*-
"""API 端点。起真实服务打真实请求——mock 掉 HTTP 就测不出路由和序列化的问题。"""
import json
import os
import threading
import unittest
import urllib.request

from server import make_server

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))


class TestAPI(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        cls.httpd = make_server(port=0, db=os.path.join(BASE, 'bank.db'))
        cls.port = cls.httpd.server_address[1]
        threading.Thread(target=cls.httpd.serve_forever, daemon=True).start()

    @classmethod
    def tearDownClass(cls):
        cls.httpd.shutdown()

    def get(self, path):
        with urllib.request.urlopen(f'http://127.0.0.1:{self.port}{path}') as r:
            return json.loads(r.read())

    def post(self, path, body):
        req = urllib.request.Request(
            f'http://127.0.0.1:{self.port}{path}',
            data=json.dumps(body).encode(), headers={'Content-Type': 'application/json'})
        with urllib.request.urlopen(req) as r:
            return json.loads(r.read())

    def test_取题默认一页40道(self):
        d = self.get('/api/questions')
        self.assertEqual(len(d['questions']), 40)
        self.assertEqual(d['total'], 1000)

    def test_取题带答案和解析(self):
        q = self.get('/api/questions?limit=1')['questions'][0]
        for f in ('id', 'n', 'module', 'type', 'stem', 'options', 'answer', 'explanation'):
            self.assertIn(f, q)

    def test_按模块筛(self):
        d = self.get('/api/questions?module=教育学')
        self.assertTrue(all(q['module'] == '教育学' for q in d['questions']))

    def test_按类型筛(self):
        # 别写死 1000——第 4 步会加判断题和多选题，写死了到时候必红。
        # 比的是「筛出来的都真是这个题型」+「数量和不带筛选时一致」。
        d = self.get('/api/questions?type=single&limit=1000')
        self.assertTrue(all(q['type'] == 'single' for q in d['questions']))
        self.assertEqual(d['total'], len(d['questions']))

    def test_分页不重叠(self):
        a = {q['id'] for q in self.get('/api/questions?limit=40&offset=0')['questions']}
        b = {q['id'] for q in self.get('/api/questions?limit=40&offset=40')['questions']}
        self.assertEqual(a & b, set())

    def test_作答返回判分与解析(self):
        q = self.get('/api/questions?limit=1')['questions'][0]
        wrong = next(k for k in 'ABCD' if k != q['answer'])
        d = self.post('/api/attempts', {'qid': q['id'], 'chosen': wrong})
        self.assertFalse(d['correct'])
        self.assertEqual(d['answer'], q['answer'])
        self.assertTrue(d['explanation'])

    def test_错题本连题一起返回(self):
        d = self.get('/api/wrong')
        self.assertIn('questions', d)


if __name__ == '__main__':
    unittest.main()
```

- [ ] **步骤 2：跑测试，确认它失败**

```bash
python3 -m unittest tests.test_server -v
```

预期：`ModuleNotFoundError: No module named 'server'`。

- [ ] **步骤 3：写 `server.py`**

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""本地服务：读 bank.db，提供 JSON API + 静态文件。

零第三方依赖。每请求开一个连接，不共享连接对象。
    python3 server.py            # 0.0.0.0:8000，手机连同一 WiFi 可访问
"""
import argparse
import json
import os
import socket
import sqlite3
import sys
import urllib.parse
from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer

import banklib

BASE = os.path.dirname(os.path.abspath(__file__))
WEB = os.path.join(BASE, 'web')
DB = os.path.join(BASE, 'bank.db')

MIME = {'.html': 'text/html; charset=utf-8', '.css': 'text/css; charset=utf-8',
        '.js': 'application/javascript; charset=utf-8', '.json': 'application/json',
        '.svg': 'image/svg+xml', '.ico': 'image/x-icon'}


def make_handler(db_path):
    class Handler(BaseHTTPRequestHandler):
        def log_message(self, fmt, *a):
            pass  # 别把每个静态请求都打到终端

        # ---- 工具 ----
        def _conn(self):
            c = sqlite3.connect(db_path)
            c.execute('PRAGMA foreign_keys = ON')
            return c

        def _send(self, obj, code=200):
            body = json.dumps(obj, ensure_ascii=False).encode('utf-8')
            self.send_response(code)
            self.send_header('Content-Type', 'application/json; charset=utf-8')
            self.send_header('Content-Length', str(len(body)))
            self.end_headers()
            self.wfile.write(body)

        def _body(self):
            n = int(self.headers.get('Content-Length') or 0)
            return json.loads(self.rfile.read(n) or b'{}')

        def _file(self, rel):
            path = os.path.normpath(os.path.join(WEB, rel.lstrip('/')))
            if not path.startswith(WEB):          # 挡住 ../ 穿越
                return self._send({'error': 'forbidden'}, 403)
            if os.path.isdir(path):
                path = os.path.join(path, 'index.html')
            if not os.path.isfile(path):
                return self._send({'error': 'not found', 'path': rel}, 404)
            data = open(path, 'rb').read()
            self.send_response(200)
            self.send_header('Content-Type',
                             MIME.get(os.path.splitext(path)[1], 'application/octet-stream'))
            self.send_header('Content-Length', str(len(data)))
            self.send_header('Cache-Control', 'no-store')   # 改完刷新就见效
            self.end_headers()
            self.wfile.write(data)

        # ---- 路由 ----
        def do_GET(self):
            u = urllib.parse.urlparse(self.path)
            q = urllib.parse.parse_qs(u.query)
            one = lambda k: (q.get(k) or [None])[0]

            if u.path == '/api/questions':
                conn = self._conn()
                try:
                    return self._send(banklib.list_questions(
                        conn, section=one('section'), module=one('module'),
                        qtype=one('type'),
                        limit=int(one('limit') or 40), offset=int(one('offset') or 0)))
                finally:
                    conn.close()

            if u.path == '/api/wrong':
                conn = self._conn()
                try:
                    conn.row_factory = sqlite3.Row
                    rows = conn.execute(
                        "SELECT q.id,q.n,q.module,q.type,q.stem,q.options,q.answer,"
                        "q.explanation,w.chosen,w.wrong_count "
                        "FROM wrong w JOIN questions q ON q.id=w.qid "
                        "WHERE w.resolved=0 ORDER BY w.last_at DESC").fetchall()
                    out = []
                    for r in rows:
                        d = dict(r)
                        d['options'] = json.loads(d['options'])
                        out.append(d)
                    return self._send({'total': len(out), 'questions': out})
                finally:
                    conn.close()

            if u.path == '/api/stats':
                conn = self._conn()
                try:
                    total = conn.execute('SELECT COUNT(*) FROM questions').fetchone()[0]
                    done = conn.execute('SELECT COUNT(*) FROM attempts').fetchone()[0]
                    right = conn.execute(
                        'SELECT COUNT(*) FROM attempts WHERE correct=1').fetchone()[0]
                    by_mod = conn.execute(
                        "SELECT q.module, COUNT(*) t, SUM(a.correct) r "
                        "FROM attempts a JOIN questions q ON q.id=a.qid "
                        "GROUP BY q.module ORDER BY q.module").fetchall()
                    return self._send({
                        'total': total, 'done': done, 'right': right,
                        'rate': round(right / done * 100) if done else 0,
                        'by_module': [{'module': m, 'total': t, 'right': r or 0}
                                      for m, t, r in by_mod]})
                finally:
                    conn.close()

            return self._file(u.path)

        def do_POST(self):
            u = urllib.parse.urlparse(self.path)
            if u.path == '/api/attempts':
                b = self._body()
                conn = self._conn()
                try:
                    return self._send(banklib.record_attempt(conn, b['qid'], b.get('chosen', '')))
                except KeyError as e:
                    return self._send({'error': str(e)}, 404)
                finally:
                    conn.close()
            return self._send({'error': 'not found'}, 404)

        def do_DELETE(self):
            u = urllib.parse.urlparse(self.path)
            conn = self._conn()
            try:
                if u.path == '/api/attempts':
                    conn.execute('DELETE FROM attempts')
                    conn.execute('DELETE FROM wrong')
                    conn.commit()
                    return self._send({'ok': True})
                if u.path == '/api/wrong':
                    conn.execute('DELETE FROM wrong')
                    conn.commit()
                    return self._send({'ok': True})
            finally:
                conn.close()
            return self._send({'error': 'not found'}, 404)

    return Handler


def make_server(port=8000, db=DB, host='0.0.0.0'):
    return ThreadingHTTPServer((host, port), make_handler(db))


def lan_ip():
    """拿本机在局域网里的地址，方便手机访问。"""
    s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    try:
        s.connect(('8.8.8.8', 80))
        return s.getsockname()[0]
    except OSError:
        return '127.0.0.1'
    finally:
        s.close()


if __name__ == '__main__':
    ap = argparse.ArgumentParser()
    ap.add_argument('-p', '--port', type=int, default=8000)
    ap.add_argument('--db', default=DB)
    a = ap.parse_args()
    print(f'服务已起：http://127.0.0.1:{a.port}')
    print(f'手机访问：http://{lan_ip()}:{a.port}   （需同一 WiFi）')
    make_server(a.port, a.db).serve_forever()
```

- [ ] **步骤 4：跑测试，确认通过**

```bash
python3 -m unittest tests.test_server -v
```

预期：7 个测试全 PASS。

- [ ] **步骤 5：突变验证**

把 `list_questions` 里的 `ORDER BY n LIMIT ? OFFSET ?` 改成 `ORDER BY n LIMIT ?` 并丢掉 offset，重跑，预期 `test_分页不重叠` 变红（两页会完全重合）。还原。

- [ ] **步骤 6：手动起一次服务看看**

```bash
python3 server.py &
curl -s 'http://127.0.0.1:8000/api/questions?limit=2' | python3 -m json.tool | head -30
curl -s 'http://127.0.0.1:8000/api/stats' | python3 -m json.tool
kill %1
```

- [ ] **步骤 7：提交**

```bash
git add server.py tests/test_server.py
git commit -m "服务层：HTTP API 与静态文件服务"
```

---

### 任务 7：抽出共享资源（CSS / 导航 / 取数）

**这一步不改行为**，只是把 `考点体系.html` 里的 CSS 和导航逻辑抽成 `web/assets/` 下的文件，供后面 15 个页面共用。

**文件：**
- 创建：`web/assets/app.css`、`web/assets/nav.js`、`web/assets/bank.js`
- 创建：`tools/extract_assets.py`

**接口：**
- 产出：
  - `nav.js` 暴露 `NAV` 数组（`{id, label, href}`）和 `mountNav()`，自动给当前页高亮
  - `bank.js` 暴露 `api.get(path)` / `api.post(path, body)` / `api.del(path)`

- [ ] **步骤 1：写抽取脚本**

创建 `tools/extract_assets.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""从 考点体系.html 抽出共享 CSS 与导航数据，生成 web/assets/ 下的文件。

只做一次。之后 CSS 直接改 web/assets/app.css。
"""
import json
import os
import re

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SRC = os.path.join(BASE, '考点体系.html')
OUT = os.path.join(BASE, 'web', 'assets')


def main():
    h = open(SRC, encoding='utf-8').read()
    os.makedirs(OUT, exist_ok=True)

    # <style> 整块搬走
    css = re.search(r'<style>([\s\S]*?)</style>', h).group(1)
    open(os.path.join(OUT, 'app.css'), 'w', encoding='utf-8').write(css.strip() + '\n')
    print('app.css', len(css), '字符')

    # 导航项：id + 文案，顺序就是页面顺序
    items = []
    seen = set()
    for m in re.finditer(r'<a href="#([\w-]+)"[^>]*>([^<]+)</a>', h):
        if m.group(1) in seen:
            continue
        seen.add(m.group(1))
        items.append({'id': m.group(1), 'label': m.group(2)})
    # 错题本是新增页，插在刷题之后
    if not any(i['id'] == 'wrong' for i in items):
        at = next((i for i, x in enumerate(items) if x['id'] == 'mock'), len(items))
        items.insert(at, {'id': 'wrong', 'label': '错题本'})

    js = ('// 导航。加页面只改这个数组——每个页面都靠它注入导航，不重复写 HTML。\n'
          'const NAV = ' + json.dumps(items, ensure_ascii=False, indent=2) + ';\n')
    open(os.path.join(OUT, 'nav-data.js'), 'w', encoding='utf-8').write(js)
    print('nav-data.js', len(items), '项')


if __name__ == '__main__':
    main()
```

- [ ] **步骤 2：跑抽取脚本**

```bash
python3 tools/extract_assets.py
wc -c web/assets/app.css web/assets/nav-data.js
```

预期：两个文件都生成，导航 15 项（14 个原有 + 错题本）。

- [ ] **步骤 3：写 `nav.js`**

```javascript
// 导航注入 + 主题切换。每个页面只需 <script src="assets/nav.js"></script>
(function () {
  const here = document.body.dataset.page || '';
  const nav = document.createElement('nav');
  nav.className = 'nav';
  nav.innerHTML =
    '<div class="nav-inner">' +
    '<span class="nav-brand">唐山教考 2026</span>' +
    '<ul class="nav-list">' +
    NAV.map(function (n) {
      const href = n.id === 'index' ? 'index.html' : n.id + '.html';
      return '<li><a href="' + href + '"' +
             (n.id === here ? ' class="active"' : '') + '>' + n.label + '</a></li>';
    }).join('') +
    '</ul>' +
    '<button class="theme-btn" id="themeBtn" aria-label="切换主题">◐</button>' +
    '</div>';
  document.body.insertBefore(nav, document.body.firstChild);

  // 三态主题：跟随系统 → 亮 → 暗
  const KEY = 'jiaokao-theme';
  const saved = localStorage.getItem(KEY);
  if (saved) document.documentElement.dataset.theme = saved;
  document.getElementById('themeBtn').addEventListener('click', function () {
    const cur = document.documentElement.dataset.theme;
    const next = cur === 'dark' ? 'light' : cur === 'light' ? '' : 'dark';
    if (next) { document.documentElement.dataset.theme = next; localStorage.setItem(KEY, next); }
    else { delete document.documentElement.dataset.theme; localStorage.removeItem(KEY); }
  });
})();
```

- [ ] **步骤 4：写 `bank.js`**

```javascript
// 取数封装。所有页面共用，错误统一抛成可读的中文。
const api = {
  async _req(path, opts) {
    const r = await fetch(path, opts);
    if (!r.ok) {
      let msg = r.status + ' ' + r.statusText;
      try { msg = (await r.json()).error || msg; } catch (e) { /* 不是 JSON 就用状态码 */ }
      throw new Error(msg);
    }
    return r.json();
  },
  get(path) { return this._req(path); },
  post(path, body) {
    return this._req(path, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    });
  },
  del(path) { return this._req(path, { method: 'DELETE' }); },
};
```

- [ ] **步骤 5：提交**

```bash
git add tools/extract_assets.py web/assets/
git commit -m "拆页：抽出共享样式与导航"
```

---

### 任务 8：拆出 15 个内容页 + 首页壳

**文件：**
- 创建：`web/_template.html`
- 创建：`tools/split_pages.py`
- 创建：`web/*.html`（15 个内容页 + `index.html`）

**接口：**
- 消费：`web/assets/*`
- 产出：每个页面有正确的 `<body data-page="...">`，导航由 `nav.js` 注入

- [ ] **步骤 1：写页面模板**

创建 `web/_template.html`：

```html
<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>__TITLE__ · 唐山教考 2026</title>
<link rel="stylesheet" href="assets/app.css">
</head>
<body data-page="__ID__">
<main class="wrap">
__BODY__
</main>
<script src="assets/nav-data.js"></script>
<script src="assets/nav.js"></script>
__SCRIPTS__
</body>
</html>
```

- [ ] **步骤 2：写拆页脚本**

创建 `tools/split_pages.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""一次性：把 考点体系.html 的每个 <section class="view" id="X"> 拆成 web/X.html。

section 内部的 HTML 原样搬，不重写——避免搬的过程中悄悄改了内容。
"""
import os
import re

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SRC = os.path.join(BASE, '考点体系.html')
OUT = os.path.join(BASE, 'web')

# 哪些页面需要额外脚本
SCRIPTS = {
    'quiz': '<script src="assets/quiz.js"></script>\n',
    'mock': '<script src="assets/mock.js"></script>\n',
    'wrong': '<script src="assets/wrong.js"></script>\n',
}

# 每个 section 的 <title>
TITLES = {
    'direction': '备考方向', 'compare': '大纲比对', 'edu': '教育类专业能力',
    'pub': '公共基础知识', 'news': '时政要点', 'local': '唐山政策与市情',
    'school': '校情·唐山工业职业技术大学', 'counselor': '高校辅导员',
    'quiz': '刷题', 'mock': '模拟考试', 'mnemonic': '记忆口诀',
    'interview': '面试', 'sprint': '冲刺计划', 'material': '资料',
}


def main():
    h = open(SRC, encoding='utf-8').read()
    tpl = open(os.path.join(OUT, '_template.html'), encoding='utf-8').read()
    os.makedirs(OUT, exist_ok=True)

    # 切成 (id, inner_html) 若干段
    sections = []
    for m in re.finditer(
            r'<section class="view" id="([\w-]+)"[^>]*>([\s\S]*?)</section>', h):
        sections.append((m.group(1), m.group(2)))

    if not sections:
        raise SystemExit('没找到任何 <section class="view">，源文件结构变了？')

    for sid, inner in sections:
        # 去掉 hidden 属性——拆页之后每页只显示自己，不再靠 hidden 切视图
        inner = re.sub(r'\s+hidden(?=[\s>])', '', inner, count=0)
        page = (tpl
                .replace('__TITLE__', TITLES.get(sid, sid))
                .replace('__ID__', sid)
                .replace('__BODY__', inner.strip())
                .replace('__SCRIPTS__', SCRIPTS.get(sid, '')))
        open(os.path.join(OUT, sid + '.html'), 'w', encoding='utf-8').write(page)
        print(f'  {sid}.html  {len(page):>7} 字符')

    # 首页壳：导航 + 总览占位
    idx = (tpl.replace('__TITLE__', '总览')
              .replace('__ID__', 'index')
              .replace('__BODY__', '<h1>唐山教考 2026</h1>\n'
                       '<p class="lead">从导航栏进入各分类。</p>\n'
                       '<div class="quick" id="quick"></div>')
              .replace('__SCRIPTS__', '<script src="assets/home.js"></script>\n'))
    open(os.path.join(OUT, 'index.html'), 'w', encoding='utf-8').write(idx)
    print(f'  index.html {len(idx):>7} 字符')
    print(f'共 {len(sections) + 1} 个页面')


if __name__ == '__main__':
    main()
```

- [ ] **步骤 3：跑拆页脚本**

```bash
python3 tools/split_pages.py
ls -la web/*.html | wc -l
```

预期：16 个文件（15 内容页 + index）。

- [ ] **步骤 4：肉眼验一个页面**

```bash
python3 server.py &
sleep 1
curl -s http://127.0.0.1:8000/edu.html | head -20
kill %1
```

预期：HTML 结构完整，`data-page="edu"`，能引到 `assets/app.css`。

- [ ] **步骤 5：提交**

```bash
git add web/ tools/split_pages.py
git commit -m "拆页：15 个内容页 + 首页壳"
```

---

### 任务 9：刷题引擎移植，行为等价

**这是第 2 步风险最高的任务。** 现有 99 条冒烟断言必须全绿。

**文件：**
- 创建：`web/assets/quiz.js`
- 修改：`tests/smoke.js`（从 `tools/smoke.js` 迁来）
- 修改：`web/quiz.html`

**接口：**
- 消费：`bank.js` 的 `api`、`/api/questions`、`/api/attempts`
- 产出：`quiz.js` 暴露 `render(resetPaging)` / `matched()` / `stats()`，名字与现有内联脚本一致——冒烟测试直接调它们

- [ ] **步骤 1：把冒烟测试迁进仓库并改成读新页面**

```bash
git mv tools/smoke.js tests/smoke.js
```

**三处改动。**

**① 题库来源换成 `题库.json`。** 它和 DB 的一致性由 `test_roundtrip.py` 保证，冒烟测试不必再连库，也就不依赖 Python 环境。

**② 脚本来源换成拆出来的 js 文件。** 原来是一整块内联 `<script>` 可以整个 `eval`；现在拆成了三个文件，要按顺序 eval。注意：`nav.js` 会往 DOM 里插导航，桩里可以照常执行（它只调 `createElement` / `insertBefore`），但**执行前 `document.body` 必须已经存在**。

```javascript
const path = require('path');
const WEB = path.join(__dirname, '..', 'web');

const HTML = fs.readFileSync(path.join(WEB, 'quiz.html'), 'utf8');

// 按页面里的实际顺序加载。改 quiz.html 的 script 标签时这里要跟着改。
const SCRIPTS = ['assets/nav-data.js', 'assets/nav.js', 'assets/bank.js', 'assets/quiz.js']
  .map(rel => path.join(WEB, rel));

let SRC = '';
for (const f of SCRIPTS) {
  try { SRC += fs.readFileSync(f, 'utf8') + '\n'; }
  catch (e) { console.error(`读不到 ${f}——先跑 python3 tools/split_pages.py`); process.exit(1); }
}
```

`eval(SRC)` 那句不变。

**③ 补 `fetch` 桩。** 引擎现在异步取数，桩要顶替 `server.py` 的两个端点。**只桩冒烟测试真正用到的那两个**——多桩一个就多一处会和真实服务脱节的地方。

```javascript
/* fetch 桩：模拟 server.py 的 /api/questions 与 /api/attempts。
   冒烟测试测的是引擎逻辑，不是 HTTP——HTTP 由 tests/test_server.py 覆盖。
   这里的判分刻意走一遍和 banklib.grade 相同的规则（去重、排序、比集合），
   好让「多选题少选算错」这类断言在客户端也成立。 */
const ALLQ = JSON.parse(
  fs.readFileSync(path.join(__dirname, '..', '题库.json'), 'utf8')).questions;

const norm = s => [...new Set((s || '').trim().toUpperCase())].sort().join('');

global.fetch = async function (url, opts) {
  const u = new URL(url, 'http://stub');
  const q = u.searchParams;
  if (u.pathname === '/api/questions') {
    const mod = q.get('module'), sec = q.get('section'), typ = q.get('type');
    const list = ALLQ.filter(x =>
      (!mod || x.module === mod) && (!sec || x.section === sec) && (!typ || x.type === typ));
    const off = +(q.get('offset') || 0);
    return { ok: true, json: async () => ({
      total: list.length, questions: list.slice(off, off + +(q.get('limit') || 40)) }) };
  }
  if (u.pathname === '/api/attempts') {
    const b = JSON.parse(opts.body);
    const item = ALLQ.find(x => x.id === b.qid);
    const correct = norm(b.chosen) === norm(item.answer);
    return { ok: true, json: async () => ({
      correct, answer: item.answer, explanation: item.explanation,
      wrong_count: correct ? 0 : 1 }) };
  }
  return { ok: false, status: 404, statusText: 'not found',
           json: async () => ({ error: 'not found' }) };
};
```

> 题库里现有的 1000 题还没有 `type` 字段，`x.type === typ` 会全部落空。桩里补一句
> `const t = x.type || (x.options.length === 2 ? 'judge' : 'single')` 再比较——**别去改 `题库.json`**，
> 那会让 `test_roundtrip.py` 红。

**④ 引擎变异步了，断言要等它。** 把 `render()` 之类的调用改成 `await`，或在断言前 `await new Promise(r => setImmediate(r))` 让微任务跑完。**修的是等待方式，不是删断言。**

- [ ] **步骤 2：跑一次，确认现在全红**

```bash
node tests/smoke.js
```

预期：**大面积失败**——`web/quiz.html` 里还没有引擎。这是对的，先看它红。

- [ ] **步骤 3：写 `web/assets/quiz.js`**

把 `考点体系.html` 里刷题那段的逻辑整体搬过来，改动只有两处：

1. **取数改成异步**：`render()` 首次调用时 `await api.get('/api/questions?limit=...')`，而不是同步读内嵌数组。
2. **判分改成问服务端**：点选项后 `await api.post('/api/attempts', {qid, chosen})`，用返回的 `correct` / `answer` / `explanation` 渲染。

其余一律不动：分页 40、`SHUF` 缓存、`shuffledOpts`、`shownKey`、`remapExplain`、`isOptRef`、模块下拉、筛选、`清空记录`。

- [ ] **步骤 4：跑冒烟测试，逐个修复到全绿**

```bash
node tests/smoke.js
```

预期：**99 条断言全绿。一条都不许少。** 如果某条断言因为异步时序挂了，修的是测试的等待方式，不是删断言。

- [ ] **步骤 5：突变验证**

删掉 `matched()` 里的模块筛选行，重跑，预期模块筛选那条变红。还原。

- [ ] **步骤 6：提交**

```bash
git add web/assets/quiz.js web/quiz.html tests/smoke.js
git commit -m "拆页：刷题引擎移植到服务端取数，行为等价"
```

---

### 任务 10：模考引擎移植

**文件：**
- 创建：`web/assets/mock.js`
- 修改：`web/mock.html`
- 修改：`tests/smoke.js`

**接口：**
- 消费：`/api/mock/*`（本任务先在 `server.py` 里补上这些端点）
- 产出：与现有模考行为等价

- [ ] **步骤 1：先在 `server.py` 补模考端点**

新增：`GET /api/mock`、`POST /api/mock/start`、`POST /api/mock/answer`、`POST /api/mock/submit`。

`POST /api/mock/start` 的抽题逻辑**原样搬 `考点体系.html` 的 `mkPick()`**：`MK_PARTS` 配比、每模块内部打乱、两部分不交错、题量不足抛「题库不足: X 需要 N 只有 M」。

`state` 存 `mock_runs.state`（JSON），结构不变：`{ids, parts, answers, i, startedAt, endsAt, submitted, submittedAt}`。

- [ ] **步骤 2：先把 HTTP 辅助方法提成基类，再补模考测试**

`tests/test_server.py` 里现在只有一个 `TestAPI`。把它的 `get` / `post` / `del` 和起服务的两行提到基类 `ApiCase`，`TestAPI` 和新的 `TestMock` 都继承——**别把起服务那几行抄两遍**，抄了以后改端口就得改两处。

```python
class ApiCase(unittest.TestCase):
    """起真实服务 + HTTP 辅助。两个测试类共用，别抄第二遍。"""

    @classmethod
    def setUpClass(cls):
        cls.httpd = make_server(port=0, db=os.path.join(BASE, 'bank.db'))
        cls.port = cls.httpd.server_address[1]
        threading.Thread(target=cls.httpd.serve_forever, daemon=True).start()

    @classmethod
    def tearDownClass(cls):
        cls.httpd.shutdown()

    def _req(self, method, path, body=None):
        url = f'http://127.0.0.1:{self.port}{path}'
        req = urllib.request.Request(
            url, method=method,
            data=json.dumps(body).encode() if body is not None else None,
            headers={'Content-Type': 'application/json'})
        with urllib.request.urlopen(req) as r:
            return json.loads(r.read())

    def get(self, path):
        return self._req('GET', path)

    def post(self, path, body):
        return self._req('POST', path, body)

    def del_(self, path):
        return self._req('DELETE', path)


def _lookup(qid, col):
    """从库里单查一个字段。测试要断言题目属性时用——不绕道 API，避免
    拿被测代码的输出当自己断言的依据。"""
    conn = sqlite3.connect(os.path.join(BASE, 'bank.db'))
    try:
        return conn.execute(
            f'SELECT {col} FROM questions WHERE id=?', (qid,)).fetchone()[0]
    finally:
        conn.close()


class TestMock(ApiCase):
    def setUp(self):
        # 每个测试从空错题本开始，否则测试之间互相污染，红绿不可复现
        self.del_('/api/wrong')

    def seg_of(self, qid):
        """这道题属于哪一段。MK_PARTS 是 公基段 + 教基段，这里粗分成两段就够。"""
        return 'pub' if _lookup(qid, 'section') == 'pub' else 'edu'

    def test_抽题120道不重复(self):
        d = self.post('/api/mock/start', {})
        self.assertEqual(len(d['ids']), 120)
        self.assertEqual(len(set(d['ids'])), 120)

    def test_两部分不交错(self):
        d = self.post('/api/mock/start', {})
        segs = [self.seg_of(i) for i in d['ids']]
        # 两段的话序列只该切换 1 次。切了 2 次以上就是公基/教基交错——
        # 真实卷子是按部分连排的，交错会让考生来回跳。
        switches = sum(1 for a, b in zip(segs, segs[1:]) if a != b)
        self.assertEqual(switches, 1, f'模块段被打断，共 {switches + 1} 段')

    def test_答题期间不判分不进错题本(self):
        before = len(self.get('/api/wrong')['questions'])
        d = self.post('/api/mock/start', {})
        self.post('/api/mock/answer', {'qid': d['ids'][0], 'chosen': 'A'})
        self.assertEqual(len(self.get('/api/wrong')['questions']), before)

    def test_交卷才并进错题本(self):
        d = self.post('/api/mock/start', {})
        for qid in d['ids'][:5]:
            self.post('/api/mock/answer', {'qid': qid, 'chosen': 'A'})
        r = self.post('/api/mock/submit', {})
        self.assertIn('score', r)
        self.assertIn('by_module', r)
```

记得在文件顶部 `import sqlite3`。

- [ ] **步骤 3：跑测试，确认失败**

```bash
python3 -m unittest tests.test_server -v
```

预期：新增的模考测试全失败（端点还没写）。

- [ ] **步骤 4：实现端点，跑到通过**

```bash
python3 -m unittest tests.test_server -v
```

预期：全 PASS。

- [ ] **步骤 5：写 `web/assets/mock.js`**

搬 `考点体系.html` 的模考逻辑。**`mkCommit()` 的三条路径一条都不能漏**（手动交卷、页内超时、页面关着时过期）——现在这三条都走服务端 `POST /api/mock/submit`，天然只有一份实现。这一条要在 `mock.js` 里注释写明。

- [ ] **步骤 6：跑冒烟测试**

```bash
node tests/smoke.js
```

预期：模考那段断言全绿，总数仍是 99 条。

- [ ] **步骤 7：突变验证**

把服务端 `submit` 里合并 `attempts` 的那一步去掉，重跑 `python3 -m unittest tests.test_server -v`，预期 `test_交卷才并进错题本` 变红。还原。

- [ ] **步骤 8：提交**

```bash
git add server.py web/assets/mock.js web/mock.html tests/
git commit -m "拆页：模考引擎移植到服务端，三条交卷路径统一"
```

---

### 任务 11：旧记录迁移与收尾

**文件：**
- 修改：`server.py`（`POST /api/migrate/legacy`）
- 修改：`web/index.html`、创建 `web/assets/home.js`

- [ ] **步骤 1：写迁移端点与测试**

端点收 `{answers: {qid: key}, wrong: {qid: n}}`，写进 `attempts` / `wrong`，返回导入条数。

测试：构造一份假的老数据 POST 进去，断言 `attempts` 和 `wrong` 里出现了对应的行。

- [ ] **步骤 2：在首页放「导入旧版记录」按钮**

`home.js` 里：点按钮 → 读 `localStorage['jiaokao-answers-2026']` 和 `['jiaokao-wrong-2026']` → POST → 提示结果。**只在两个 key 都存在时才显示这个按钮。**

- [ ] **步骤 3：手动验一次**

用浏览器打开 `考点体系.html`（旧页面），再打开 `http://127.0.0.1:8000/`，点导入，确认刷题页的「已做」数字对得上。

- [ ] **步骤 4：跑全部测试**

```bash
python3 -m unittest discover tests -v && node tests/smoke.js
```

预期：Python 全绿 + 冒烟 99 条全绿。

- [ ] **步骤 5：提交**

```bash
git add server.py web/ tests/
git commit -m "拆页：旧 localStorage 记录迁移入口"
```

---

# 第 3 步：题型扩展与错题本页

---

### 任务 12：判断题（禁止乱序）

**文件：**
- 修改：`web/assets/quiz.js`
- 修改：`tests/smoke.js`

**接口：**
- 产出：`shuffledOpts(q)` 对 `q.type === 'judge'` 返回原序

- [ ] **步骤 1：先写失败的测试**

在 `tests/smoke.js` 追加：

```javascript
console.log('\n【判断题 · 禁止乱序】');
{
  const jq = { id:'test-j1', n:9991, module:'教育学', type:'judge',
               stem:'判断题干', options:[{key:'A',text:'正确'},{key:'B',text:'错误'}],
               answer:'A', explanation:'解析' };
  const order = shuffledOpts(jq);
  ok(order.length === 2, '判断题两个选项');
  ok(order[0].key === 'A' && order[1].key === 'B',
     '判断题不乱序——对/错的先后有语义，打乱会让解析指错');

  // 对照组：单选题必须照常乱序。没有这一条，上面那句在「乱序被整个关掉」
  // 的情况下也会绿——那就成了假绿。
  const singles = BANK.questions.filter(q => q.type === 'single').slice(0, 20);
  const someShuffled = singles.some(q => {
    const got = shuffledOpts(q).map(o => o.key).join('');
    return got !== q.options.map(o => o.key).join('');
  });
  ok(someShuffled, '单选题仍然乱序——否则「判断题不乱序」这条是假绿');
}
```

- [ ] **步骤 2：跑测试，确认失败**

```bash
node tests/smoke.js
```

预期：判断题那条变红。

- [ ] **步骤 3：改 `shuffledOpts`**

```javascript
function shuffledOpts(q){
  // 判断题不能乱序：「正确」必须在「错误」前面，打乱了解析里的 A/B 就指错。
  if(q.type==='judge') return q.options;
  if(!SHUF[q.id]){ /* 原有的洗牌逻辑不动 */ }
  return SHUF[q.id].map(function(i){ return q.options[i]; });
}
```

- [ ] **步骤 4：跑测试，确认通过**

```bash
node tests/smoke.js
```

- [ ] **步骤 5：突变验证**

删掉 `if(q.type==='judge') return q.options;` 这一行，重跑，预期判断题那条变红。还原。

- [ ] **步骤 6：判断题的界面**

`cardHTML` 里：判断题不显示 A/B 字母，两个按钮直接写「正确 / 错误」。补一条断言检查渲染出的 HTML 里没有 `data-k="A"` 的字样。

- [ ] **步骤 7：提交**

```bash
git add web/assets/quiz.js tests/smoke.js
git commit -m "题型：判断题禁止乱序，界面不显示字母"
```

---

### 任务 13：多选题交互

**文件：**
- 修改：`web/assets/quiz.js`
- 修改：`tests/smoke.js`

- [ ] **步骤 1：先写失败的测试**

```javascript
console.log('\n【多选题 · 勾选后确认】');
{
  const mq = { id:'test-m1', n:9992, module:'教育学', type:'multi', stem:'多选题干',
               options:[{key:'A',text:'甲'},{key:'B',text:'乙'},
                        {key:'C',text:'丙'},{key:'D',text:'丁'}],
               answer:'ABD', explanation:'故选 ABD。' };
  // 1. 点第一项不应立刻判分
  // 2. 勾两项后点「确认作答」才发请求
  // 3. 少选算错
}
```

三条断言：点击不立即提交、确认后提交的是勾选的字母串、判分结果与 `grade` 一致。

- [ ] **步骤 2：跑测试，确认失败**

```bash
node tests/smoke.js
```

- [ ] **步骤 3：实现**

- 多选题选项渲染成可多选（点击切换选中态，不提交）
- 下方出现「确认作答」按钮，点击时把选中的字母**排序后拼成字符串**提交
- 提交后锁定，按返回值渲染对错和解析
- 解析字母重映射用现有的 `remapExplain`（任务 14 会扩它）

- [ ] **步骤 4：跑测试，确认通过**

- [ ] **步骤 5：突变验证**

把「排序后拼接」改成「按点击顺序拼接」，重跑——如果测试没红，说明测试没覆盖顺序，补一条（先点 D 再点 A，提交的应是 `AD` 而不是 `DA`）。还原。

- [ ] **步骤 6：提交**

```bash
git add web/assets/quiz.js tests/smoke.js
git commit -m "题型：多选题勾选与确认作答"
```

---

### 任务 14：解析字母重映射支持多字母

**风险最高的一个任务。** 现有的 10 个例外必须逐条确认没被误伤。

**文件：**
- 修改：`web/assets/quiz.js` 的 `isOptRef` / `remapExplain`
- 修改：`tools/remap_probe.js`
- 修改：`tests/smoke.js`

- [ ] **步骤 1：先把探针指向新引擎，再扩正则**

`tools/remap_probe.js` 现在从 `考点体系.html` 里抠 `remapExplain` / `isOptRef`。这两个函数已经搬到 `web/assets/quiz.js` 了，探针的取数路径要跟着改——**不改的话它还在测旧文件，扩正则扩了个寂寞**。

改法照抄任务 9 步骤 1 的 `SCRIPTS` 读取方式，把源换成 `web/assets/quiz.js`。

然后把正则从 `[ABCD]` 改成 `[ABCD]{1,4}`，整体匹配连续字母串，**逐字母重映射后原样拼回**。

跑一遍：

```bash
node tools/remap_probe.js
```

**记录输出**：`改了字的题` 数字、`【故意不换】的字母上下文` 的完整列表。这是改动前的基线。

- [ ] **步骤 2：逐条核对「故意不换」的清单**

现在有 10 个例外（`p16`/`p228` 的 ABC 理论、`s123` 的 `（B档）` 等）。扩了正则之后这个清单**必须还是这些**，一条都不能多。多出来的就是误伤，要补规则排除。

把核对结果写进 `出题范围.md` 第九节。

- [ ] **步骤 3：改 `isOptRef` 支持多字母**

新增三种写法：
- `故选 ABD` / `应选 AC`
- `A、C 两项` / `A 和 C`
- `AB 两项` / `ACD 三项`

**`ABC 理论` 那类必须继续排除**——现有规则里 `^\s*(正确|错误|两项|三项|都|均|也|全对)` 这些要继续生效，并补上排除「理论」「模型」「阶段」等后接词。

- [ ] **步骤 4：跑探针，与基线比对**

```bash
node tools/remap_probe.js
```

预期：`判定为指代却没换` = 0；`【故意不换】` 的清单与步骤 1 记录的**完全一致**。

- [ ] **步骤 5：给冒烟测试补断言**

- 多选解析 `故选 ABD` 在乱序后指向正确的显示字母
- `ABC 理论` 的 A/B/C **没有**被改（沿用现有 p16 那条断言的写法）

- [ ] **步骤 6：跑测试 + 突变验证**

删掉新增的多字母规则，重跑，预期多选解析那条变红。还原。

- [ ] **步骤 7：提交**

```bash
git add web/assets/quiz.js tools/remap_probe.js tests/smoke.js 出题范围.md
git commit -m "题型：解析字母重映射支持多字母指代"
```

---

### 任务 15：错题本独立页

**文件：**
- 创建：`web/assets/wrong.js`
- 修改：`web/wrong.html`
- 修改：`tests/test_server.py`、`tests/smoke.js`

- [ ] **步骤 1：先写失败的测试**

服务端：`POST /api/wrong/<qid>/resolve` 后，该题不再出现在 `/api/wrong`。

冒烟：错题本页渲染出的题目，多选题要显示「你选 AC，正确 ABD」。

- [ ] **步骤 2：跑测试，确认失败**

- [ ] **步骤 3：实现 `wrong.js`**

渲染 `GET /api/wrong`：题干、你的错选、正确答案、解析（走 `remapExplain`）、错了几次、做题入口。
按模块分组，支持「只显示错 2 次以上的」「标记已订正」「重做这道题」。

- [ ] **步骤 4：跑测试，确认通过**

- [ ] **步骤 5：突变验证**

把服务端 `/api/wrong` 的 `WHERE w.resolved=0` 去掉，重跑，预期「标记已订正后不再出现」那条变红。还原。

- [ ] **步骤 6：提交**

```bash
git add web/assets/wrong.js web/wrong.html tests/
git commit -m "错题本：独立成页，支持标记订正与重做"
```

---

### 任务 16：模考「含多选/判断」开关

**文件：**
- 修改：`server.py` 的抽题逻辑
- 修改：`web/assets/mock.js`、`web/mock.html`
- 修改：`tests/test_server.py`

- [ ] **步骤 1：先写失败的测试**

```python
def test_默认仍是120道全单选(self):
    d = self.post('/api/mock/start', {})
    types = {self.type_of(i) for i in d['ids']}
    self.assertEqual(types, {'single'})

def test_开关打开后掺入判断和多选(self):
    d = self.post('/api/mock/start', {'withTypes': True})
    types = {self.type_of(i) for i in d['ids']}
    self.assertIn('judge', types)
    self.assertIn('multi', types)

def test_练兵版与默认版题量相同(self):
    a = self.post('/api/mock/start', {})
    b = self.post('/api/mock/start', {'withTypes': True})
    self.assertEqual(len(a['ids']), len(b['ids']))
```

- [ ] **步骤 2：跑测试，确认失败**

- [ ] **步骤 3：实现**

`POST /api/mock/start` 接受 `withTypes`。为 `True` 时配比从「模块→题量」升级为「模块→题量→各题型题量」，总题量仍是 120。`state` 里记下 `withTypes`，成绩单上标明本场是哪种版本。

- [ ] **步骤 4：配比表旁加开关**

`mock.html` 的配比表旁加一个 checkbox「含多选/判断（练兵）」，默认不勾。勾选状态只影响下一次开考。

- [ ] **步骤 5：跑测试 + 突变验证**

把 `withTypes` 参数忽略掉（永远当 False），重跑，预期 `test_开关打开后掺入判断和多选` 变红。还原。

- [ ] **步骤 6：跑全部测试**

```bash
python3 -m unittest discover tests -v && node tests/smoke.js
```

- [ ] **步骤 7：提交**

```bash
git add server.py web/ tests/
git commit -m "模考：练兵版开关，可掺入判断题与多选题"
```

---

### 任务 17：收尾

- [ ] **步骤 1：更新 `出题范围.md`**

- 第一节的「**所有题都必须是四选一的单选**」已经不成立——改成三题型的说明
- 加上第三批 id 段（3xx）
- 第九节补上多字母指代的规则与例外清单

- [ ] **步骤 2：更新 `README.md`**

- 目录表加 `bank.db` / `server.py` / `web/`，`考点体系.html` 标注为**旧版**
- 「怎么用」改成起服务
- 测试与工具那节改成新路径

- [ ] **步骤 3：跑全部测试，确认全绿**

```bash
python3 -m unittest discover tests -v && node tests/smoke.js
```

- [ ] **步骤 4：提交并推送**

```bash
git add -A
git commit -m "文档：同步三题型、新目录结构与使用方式"
git push
```

---

## 完成后

第 4 步（扩充 1000 题）**另出一份计划**——它依赖这里冻结下来的 schema 和 `grade()`，而且是内容生产不是写代码，任务形态完全不同。

到那时 `考点体系.html` 才可以删。在那之前它一直是回归对照。
